# Pipecat Framework - Install first to avoid conflicts
pipecat-ai[sarvam]==0.0.95  # Confirmed working version with built-in Sarvam services

# Web Framework - Compatible versions with Pipecat
fastapi>=0.104.0,<0.116.0   # Compatible FastAPI version range
uvicorn[standard]>=0.24.0    # ASGI server with WebSocket support
websockets>=12.0             # WebSocket library for real-time communication

# Twilio
twilio>=8.10.0,<10.0.0       # Twilio SDK for voice calls and media streams

# HTTP Client - Compatible versions
httpx>=0.25.0,<0.28.0        # Modern HTTP client for API calls
aiohttp>=3.9.0,<4.0.0        # Async HTTP client/server for TTS services

# Audio Processing - Compatible with Pipecat
onnxruntime>=1.16.0,<1.21.0  # ONNX runtime for audio processing and VAD

# Configuration - Compatible versions
pydantic>=2.5.0,<3.0.0       # Data validation and settings management
pydantic-settings>=2.1.0     # Settings management with environment variables
python-dotenv>=1.0.0         # Load environment variables from .env file
pyyaml>=6.0.0                # YAML configuration file support

# Logging - Use version compatible with Pipecat
loguru>=0.7.0,<0.8.0         # Structured logging with rotation and filtering

# Utilities
python-multipart>=0.0.6      # Multipart form data parsing for FastAPI

# Production Dependencies - Flexible versions
redis>=5.0.0,<6.0.0          # Redis client for session storage and caching
aioredis>=2.0.0,<3.0.0       # Async Redis client
slowapi>=0.1.8               # Rate limiting middleware for FastAPI
prometheus-client>=0.19.0    # Metrics collection and monitoring

# Additional dependencies for stability
typing-extensions>=4.8.0     # Extended typing support for older Python versions
